#name: Build and Deploy to Azure
#
#on:
#  push:
#    branches:
#      - main
#  workflow_dispatch:  # Allow manual trigger
#
#env:
#  REGISTRY_NAME: myregistry  # Your ACR name (without .azurecr.io)
#  IMAGE_NAME: chatvault
#  APP_SERVICE_NAME: my-chatvault-app  # Your App Service name
#  RESOURCE_GROUP: my-resource-group  # Your Azure resource group
#
#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v4
#
#      - name: Set up Docker Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Log in to Azure Container Registry
#        uses: azure/docker-login@v1
#        with:
#          login-server: ${{ env.REGISTRY_NAME }}.azurecr.io
#          username: ${{ secrets.ACR_USERNAME }}
#          password: ${{ secrets.ACR_PASSWORD }}
#
#      - name: Build and push Docker image
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          push: true
#          tags: ${{ env.REGISTRY_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:latest,${{ env.REGISTRY_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
#          cache-from: type=gha
#          cache-to: type=gha,mode=max
#
#      - name: Azure Login
#        uses: azure/login@v1
#        with:
#          creds: ${{ secrets.AZURE_CREDENTIALS }}
#
#      - name: Deploy to Azure App Service
#        uses: azure/webapps-deploy@v2
#        with:
#          app-name: ${{ env.APP_SERVICE_NAME }}
#          images: ${{ env.REGISTRY_NAME }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
#
#      - name: Logout from Azure
#        run: az logout
#        if: always()
